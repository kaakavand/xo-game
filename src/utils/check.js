export const checkO1 = (nobatO) => {
    const arr = ['3' , '6' , '9']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('o barande shode')
    }
}

export const checkO2 = (nobatO) => {
    const arr = ['1' , '2' , '3']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('o barande shode')
    }
}

export const checkO3 = (nobatO) => {
    const arr = ['4' , '5' , '6']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('o barande shode')
    }
}

export const checkO4 = (nobatO) => {
    const arr = ['7' , '8' , '9']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('o barande shode')
    }
}

export const checkO5 = (nobatO) => {
    const arr = ['1' , '4' , '7']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('o barande shode')
    }
}

export const checkO6 = (nobatO) => {
    const arr = ['2' , '5' , '8']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('o barande shode')
    }
}

export const checkO7 = (nobatO) => {
    const arr = ['1' , '5' , '9']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('o barande shode')
    }
}

export const checkO8 = (nobatO) => {
    const arr = ['3' , '5' , '7']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('o barande shode')
    }
}


// ------------------------------------------ //

export const checkX1 = (nobatO) => {
    const arr = ['3' , '6' , '9']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('X barande shode')
    }
}

export const checkX2 = (nobatO) => {
    const arr = ['1' , '2' , '3']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('X barande shode')
    }
}

export const checkX3 = (nobatO) => {
    const arr = ['4' , '5' , '6']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('X barande shode')
    }
}

export const checkX4 = (nobatO) => {
    const arr = ['7' , '8' , '9']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('X barande shode')
    }
}

export const checkX5 = (nobatO) => {
    const arr = ['1' , '4' , '7']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('X barande shode')
    }
}

export const checkX6 = (nobatO) => {
    const arr = ['2' , '5' , '8']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('X barande shode')
    }
}

export const checkX7 = (nobatO) => {
    const arr = ['1' , '5' , '9']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('X barande shode')
    }
}

export const checkX8 = (nobatO) => {
    const arr = ['3' , '5' , '7']
    const res = []
    for (let i = 0; i < arr.length; i++) {
        if (nobatO.includes(arr[i])) {
            res.push(true)
        }
    }
    if (res.length == 3) {
        alert('X barande shode')
    }
}